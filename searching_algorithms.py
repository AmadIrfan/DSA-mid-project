def LinearSarch(array,searchData):
    indexArr=[]
    for i in range(0,len(array)):
        if searchData==array[i]:
            indexArr.append(i)
    return indexArr

def BinarySearch(array,search):
    print('qw')